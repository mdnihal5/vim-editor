--style=google                 # K&R brace style, clean and compact
--indent=spaces=4              # 4 spaces per indent
--attach-classes               # Attach class braces on same line
--attach-inlines               # Attach inline braces
--attach-extern-c              # Attach extern "C" braces
--attach-closing-while         # Keep while after closing brace of do
--attach-namespaces            # Attach namespace braces

--indent-switches              # Indent inside switch
--indent-cases                 # Indent case labels
--indent-namespaces            # Indent inside namespaces
--indent-preproc-block         # Indent preprocessor blocks
--indent-preproc-define        # Indent defines
--indent-preproc-cond          # Indent preprocessor conditionals
--indent-col1-comments         # Keep column 1 comments properly aligned

--pad-oper                     # Space around operators
--pad-paren-out                # Space outside parentheses
--pad-header                   # Space after if/for/while
--unpad-paren                  # No space inside parentheses

--align-pointer=type           # int* ptr style
--align-reference=name         # int &ref style

--break-blocks=all             # Blank line around blocks
--break-closing-braces         # Line break after closing brace
--keep-one-line-blocks         # Allow short { return; } style
--keep-one-line-statements     # Allow short single-line ifs

--convert-tabs                 # Tabs → spaces
--close-templates              # Fix >> spacing in templates
--remove-comment-prefix        # Clean comment alignment
--delete-empty-lines           # Remove redundant blank lines

--max-code-length=120          # Allow longer lines
--mode=c                       # Treat as C/C++ code
--lineend=linux                # LF endings (good for macOS/Linux)

